import 'package:flutter/material.dart';

/// Visitors list screen
class VisitorsListScreen extends StatelessWidget {
  const VisitorsListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Visitors')),
      body: const Center(child: Text('Visitors List Screen - Coming Soon')),
    );
  }
}
