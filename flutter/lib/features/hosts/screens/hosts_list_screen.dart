import 'package:flutter/material.dart';

/// Hosts list screen
class HostsListScreen extends StatelessWidget {
  const HostsListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Hosts')),
      body: const Center(child: Text('Hosts List Screen - Coming Soon')),
    );
  }
}
