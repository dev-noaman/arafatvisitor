import 'package:flutter/material.dart';

/// Pre-register form screen
class PreRegisterFormScreen extends StatelessWidget {
  const PreRegisterFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Pre-Register Visitor')),
      body: const Center(child: Text('Pre-Register Form Screen - Coming Soon')),
    );
  }
}
