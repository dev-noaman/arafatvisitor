import 'package:flutter/material.dart';

/// Host form screen
class HostFormScreen extends StatelessWidget {
  const HostFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Host Form')),
      body: const Center(child: Text('Host Form Screen - Coming Soon')),
    );
  }
}
