import 'package:flutter/material.dart';

/// Pre-register list screen
class PreRegisterListScreen extends StatelessWidget {
  const PreRegisterListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Pre-Register')),
      body: const Center(child: Text('Pre-Register List Screen - Coming Soon')),
    );
  }
}
