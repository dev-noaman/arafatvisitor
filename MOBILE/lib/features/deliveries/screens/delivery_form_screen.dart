import 'package:flutter/material.dart';

/// Delivery form screen
class DeliveryFormScreen extends StatelessWidget {
  const DeliveryFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Delivery Form')),
      body: const Center(child: Text('Delivery Form Screen - Coming Soon')),
    );
  }
}
